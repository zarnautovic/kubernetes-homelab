apiVersion: v1
kind: Service
metadata:
  name: vault-webhook
  namespace: obsidian-livesync
spec:
  selector:
    app.kubernetes.io/name: livesync-bridge
  ports:
    - name: http
      port: 8080
      targetPort: 8080
