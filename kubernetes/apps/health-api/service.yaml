apiVersion: v1
kind: Service
metadata:
  name: health-api
  namespace: health-api
spec:
  selector:
    app.kubernetes.io/name: health-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
