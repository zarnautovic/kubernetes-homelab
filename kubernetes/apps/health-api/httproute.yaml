apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: health-api
  namespace: health-api
spec:
  parentRefs:
    - name: main
      namespace: network
  hostnames:
    - "health.zlaya.tech"
  rules:
    # One hostname, two protection models — split at the Traefik VM:
    #   /health-metrics + /healthz  -> router WITHOUT authentik (the
    #     phone's HAE app authenticates with the Bearer token)
    #   everything else (dashboard, read APIs) -> router WITH the
    #     chain-authentik middleware
    # The Traefik split is live (verified: anonymous / gets a 302 to
    # Authentik, /healthz stays open), so the whole hostname can flow
    # to the service.
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: health-api
          port: 8080
