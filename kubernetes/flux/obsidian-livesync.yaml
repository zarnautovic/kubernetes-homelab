apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: obsidian-livesync
  namespace: flux-system
spec:
  interval: 10m
  path: ./kubernetes/apps/obsidian-livesync
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: longhorn
    - name: gateway
  decryption:
    provider: sops
    secretRef:
      name: sops-age
